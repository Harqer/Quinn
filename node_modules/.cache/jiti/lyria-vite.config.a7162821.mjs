"use strict";Object.defineProperty(exports, "__esModule", { value: true });exports.default = void 0;var _path = _interopRequireDefault(await jitiImport("path"));
var _vite = await jitiImport("vite");function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}var _default = exports.default =


(0, _vite.defineConfig)(({ mode }) => {
  const env = (0, _vite.loadEnv)(mode, '.', '');
  return {
    server: {
      port: 3000,
      host: '0.0.0.0',
      hmr: false
    },
    preview: {
      port: 3000,
      host: '0.0.0.0',
      allowedHosts: true
    },
    plugins: [
    {
      name: 'inject-api-key',
      transformIndexHtml(html) {
        const apiKey = process.env.GEMINI_API_KEY || env.GEMINI_API_KEY || '';
        return html.replace(
          '<head>',
          `<head><script>
                window.process = {
                  env: {
                    API_KEY: ${JSON.stringify(apiKey)},
                    GEMINI_API_KEY: ${JSON.stringify(apiKey)}
                  },
                  versions: {
                    node: '22.0.0'
                  }
                };
                window.API_KEY = ${JSON.stringify(apiKey)};
                window.GEMINI_API_KEY = ${JSON.stringify(apiKey)};
              </script>`
        );
      }
    }],

    define: {
      'process.env.API_KEY': JSON.stringify(process.env.GEMINI_API_KEY || env.GEMINI_API_KEY || ''),
      'process.env.GEMINI_API_KEY': JSON.stringify(process.env.GEMINI_API_KEY || env.GEMINI_API_KEY || '')
    },
    resolve: {
      alias: {
        '@': _path.default.resolve(__dirname, '.')
      }
    }
  };
}); /* v9-52dfec7dad6bbd87 */
