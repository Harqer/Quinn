package com.musically.studio.ui.theme

import androidx.compose.foundation.style.Style
import androidx.compose.material3.MaterialTheme
import androidx.compose.ui.unit.dp

object ComponentStyles {
    /**
     * Primary button style using the experimental Styles API.
     * Aligns with Material 3 Expressive standards.
     */
    val primaryButtonStyle = Style {
        // background(MaterialTheme.colorScheme.primary)
        // shape(MaterialTheme.shapes.extraLarge)
        // minHeight(56.dp)
        // Note: Styles API properties are added here as the alpha evolves
    }

    val surfaceCardStyle = Style {
        // background(MaterialTheme.colorScheme.surfaceContainer)
        // shape(MaterialTheme.shapes.large)
    }
}
