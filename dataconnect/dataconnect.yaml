specVersion: "v1"
serviceId: "musically-studio"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "musically_studio_db"
      cloudSql:
        instanceId: "musically-studio-instance"
connectorDirs: ["./connector"]
